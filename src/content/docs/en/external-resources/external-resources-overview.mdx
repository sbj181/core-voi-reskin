---
title: Source Materials
description: These materials can only be shared externally.
i18nReady: true
type: base
---
import Button from '~/components/Button.astro'
import ResourceCard from '~/components/ResourceCard.astro'
import TagFilter from '~/components/TagFilter.astro'


import resources from '~/data/resources.json'
import getRelatedResources from '~/components/getRelatedResources.tsx';


View our external source materials below. These materials can only be shared externally.

## Filter Materials

<div class="tag-filter-container">
	<TagFilter tags={['Websites', 'Downloads', 'Videos', 'Forms', 'Other']} />
</div>


<div class="resource-card-container">
  {getRelatedResources(['Websites', 'Downloads', 'Videos', 'Forms', 'Other']).map(relatedResource => (
    <ResourceCard {...relatedResource} />
  ))}
</div>